---
import HTMLLayout from "@/layouts/HTMLLayout.astro";
import { EventCard } from "@/components/event-card";
import { getEntry } from "astro:content";
const event = await getEntry("events", "rouen-france-2024")
const meetup = await getEntry("meetups", "tunis-tunisia-09-2024")
---

<HTMLLayout>
  <h1 class="font-heading">Fork it! Community</h1>
  <EventCard  event={event?.data}/>
  <EventCard  event={meetup?.data}/>
</HTMLLayout>
