import { Frame } from "@/dynamic-images/components/Frame";

export default async function () {
  return (
    <Frame width={100} height={100} style={{ padding: 8 }}>
      <div>Hello</div>
    </Frame>
  );
}
